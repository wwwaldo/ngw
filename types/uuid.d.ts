export = index;
declare function index(options: any, buf: any, offset: any): any;
declare namespace index {
  function v1(options: any, buf: any, offset: any): any;
  // Circular reference from index
  const v4: any;
}
